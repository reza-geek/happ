export	class User
    {
        User_ID             ? : number;  
        Username            ? : string ; 
        Password            ? : string ; 
        Name                ? : string ; 
        Family              ? : string ; 
        National_Code       ? : string ; 
        Mobile              ? : string ; 
        Create_DateTime     ? : string ; 
        Is_Active           ? : boolean;  
        LoginFailedCount    ? : number ; 
        LastLoginDate       ? : string ; 
    }