import { User } from "../user/User";
export class Patient
    {       
        Patient_ID  ? : number ;
        FName  ? : string ;
        LName  ? : string ;
        Birthdate  ? : string ;
        National_Code  ? : string ;
        Phone  ? : string ;
        Mobile  ? : string ;
        Gender  ? : string ;
        Insert_Date  ? : string ;
        User_ID ? : number ;
        User ? : User ;
    }
     