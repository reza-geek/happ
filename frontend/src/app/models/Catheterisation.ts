export class Catheterisation
    {
        Catheterisation_ID ? : number;
        Reception_ID ? : number;
        Dr_ID ? : number ;
        Has_Event  ? : boolean;
        Event_ID  ? : number;
        Event_Desc  ? : string;
        Catheterisation_Date ? : string ;
        User_ID ? : number ;
        Catheter_ID  ? : number;
        Catheterisation_EjectDate ? : string;
        CatheterEject_ID  ? : number ;
        Catheterisation_DateEn  ? : string;
        Catheterisation_EjectDateEn  ? : string;
    }